server:
  port: 9101
spring:
  application:
    name: sentinel-feign
  cloud:
    nacos:
      discovery:
        server-addr: server:80
    sentinel:
      log:
        dir: ./alibaba-sentinel-feign/logs
      transport:
        dashboard: server:80/sentinel-dashboard
      eager: true